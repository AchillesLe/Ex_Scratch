extends layout
block content

    h1 #{article.title}
    h5 written by #{article.author}
    p #{article.body}
    hr
    a.btn.btn-success( href = "/article/edit/" + article.id  ) Edit
    | 
    a.btn.btn-danger.delete-article(  data-id = article.id  ) Delete
    //- day lay gi
